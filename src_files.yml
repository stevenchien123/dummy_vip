# 檔案位置：您自己 fork 的 dummy_vip 專案根目錄下的 src_files.yml
dummy_vip:
  incdirs: [
    include,
  ]
  vlog_opts: [
    +define+ENABLE_DUMMY_VIP,
    -L common_cells_lib,
  ]
  files: [
    rtl/dummy_submodule.sv,
    rtl/dummy_top.sv,
  ]
  flags: [
    skip_synthesis,
  ]